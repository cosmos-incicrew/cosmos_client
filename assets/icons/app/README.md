# 앱 런처 아이콘

여기에 **`app_icon.png`** (정사각형, **1024x1024 권장**, 투명 배경 X)를 넣고
프로젝트 루트에서 아래를 실행하면 안드로이드·iOS·웹·윈도우 아이콘이 생성된다:

```bash
flutter pub get
dart run flutter_launcher_icons
```

설정은 `pubspec.yaml` 맨 아래 `flutter_launcher_icons:` 섹션에 있다.
